package com.pulse.android.sdk.replay.events

/** Pointer device type for mouse/touch interaction events. */
public enum class PointerType(public val value: Int) {
    MOUSE(0),
    PEN(1),
    TOUCH(2),
}
