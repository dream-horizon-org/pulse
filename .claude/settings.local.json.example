{
  "_comment": "Copy to settings.local.json and gitignore it. Personal overrides only.",
  "claudeMdExcludes": [
    "pulse-ui/.claude/**",
    "pulse_ai/.claude/**",
    "pulse-android-otel/.claude/**",
    "pulse-react-native-otel/.claude/**"
  ],
  "env": {
    "DEV_MODE_API_KEY": "default-project_devkey01"
  }
}
