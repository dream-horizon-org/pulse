Remove outdated or incorrect knowledge from the memory graph. Confirm before deleting.

1. Ask what to forget if not provided
2. Search for matching entities or observations with `search_nodes`
3. Show what was found and ask for explicit confirmation before deleting
4. Use `delete_observations` for specific facts, `delete_entities` for entire entities, or `delete_relations` for links
5. Confirm what was removed
