Build and verify the React frontend.

1. Change to `pulse-ui/`
2. Run `yarn install` if `node_modules` doesn't exist
3. Run `yarn build`
4. If the build fails, analyze the TypeScript or ESLint errors and suggest fixes
5. If it passes, report success
