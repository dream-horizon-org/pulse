2026-05-08 — Issue 01 complete (RcaSegmentMergePolicy deep module)
  Created: backend/server/src/main/java/.../service/rootcause/RcaSegmentMergePolicy.java
  Created: backend/server/src/test/java/.../service/rootcause/RcaSegmentMergePolicyTest.java
  19 unit tests, all green.
  Key decisions:
    - @UtilityClass static entry point mergeAndCap(baseline, hierarchicalCandidates, flatCandidates, dimensionOrder, maxSegments)
    - 1D segments passed as hierarchicalCandidates are silently dropped (not promoted to flat tier)
    - computeLift() is package-private for test access
    - Uses NumberCoercionUtils for zero-safe arithmetic
  Next: Issue 02 — wire orchestration in RootCauseService + ScreenRcaService, add HYBRID enum, service tests.

2026-05-08 — Issue 03 complete (enrichment serverRank = merged segment order)
  Touch: RcaReportEnrichmentService.sanitizeForAiReport — removed problematic_count sort; serverRank 1..N follows
    RootCauseService segment list order (hybrid merge + cap + signal gate as returned on GET).
  Touch: RootCauseSegment.serverRank javadoc; RcaReportEnrichmentServiceTest — two segment-order tests hoisted to
    top-level (Surefire in this repo was only executing outer-class @Test methods).
  Verification: mvn compiler:testCompile -Dmaven.compiler.testIncludes='**/RcaReportEnrichmentServiceTest.java'
    && mvn surefire:test -Dtest=RcaReportEnrichmentServiceTest (4 tests). Full mvn test may hit pre-existing
    Kotlin test compile errors (MetricsToAddPolymorphicJsonTest.kt) on clean builds.
  Ralph lint: pnpm ai-hero-cli internal lint — N/A (no package.json at repo root).
  Next: Issue 04 — pulse_ai schema + prompts for mode hybrid; Issue 05 — UI/docs/audit harness.

2026-05-08 — Issue 02 complete (shared hybrid merge outcome + backend pipeline closure)
  Touch: RcaHybridMergeOutcome.java — mergeForInteraction / mergeForScreen → RcaSegmentMergePolicy;
    modeFromHierarchicalTier; debug logs for candidate counts + merged 2D+/1D tier counts.
  Touch: RootCauseService.java, ScreenRcaService.java — call merge helper (no duplicated merge/mode).
  Tests: RcaHybridMergeOutcomeTest (new). Existing RootCauseServiceTest / ScreenRcaServiceTest unchanged
    behavior; full mvn test may still fail on Kotlin MetricsToAddPolymorphicJsonTest compile (pre-existing).
  Verify: mvn verify -Dmaven.test.skip=true; pulse-ui npx tsc --noEmit OK.
  Ralph: pnpm ai-hero-cli internal lint — N/A (no repo-root package.json; ERR_PNPM_NO_IMPORTER_MANIFEST_FOUND).
  Next: Issue 04 — pulse_ai `hybrid` schema + prompts (unblocks after Issue 02 per issue file); then Issue 05.

2026-05-08 — Issue 04 complete (pulse_ai hybrid contract + prompts)
  Touch: pulse_ai/schemas/root_cause.py — `mode` Literal adds **hybrid**; `serverRank` Field description = merged-order priority (2D+ before 1D in hybrid).
  Touch: pulse_ai/agents/rca/prompts.py — Input format + wire format + server-assigned rank: hybrid tier semantics; rank 1 = final merged list head, not raw problematic_count.
  Touch: pulse_ai/tests/test_root_cause_schema.py — accept flat/hierarchical/hybrid; `model_validate` rejects bad mode literal.
  Verify: pytest tests/test_root_cause_schema.py (2 tests). pulse-ui `npx tsc --noEmit` OK.
  Ralph: `pnpm ai-hero-cli internal lint` at repo root → ERR_PNPM_NO_IMPORTER_MANIFEST_FOUND (no root package.json).
  Next: Issue 05 — UI/docs + regression harness (audit scripts, runbooks per PRD user stories 12/18).

2026-05-08 — Issue 05 complete (UI hybrid type + audit failure-bucket doc + rca-db-audit mode)
  Touch: pulse-ui `ScreenRootCauseMode` + `SCREEN_RCA_MODE_LABELS` — **hybrid** aligned with
    `RootCauseRestResponse.mode`.
  Touch: **docs/rca-http-audit-failure-buckets.md** — Bucket 1 vs 2, **rank 1** under **hybrid**
    (2D+ before 1D; `segments[0]` is headline cohort). **.gitignore:** `docs/*` + un-ignore this file
    so the doc is versioned (whole `docs/` was ignored before).
  Touch: deploy/scripts/rca-db-audit.py — EXPECTATIONS `checkout_start.mode` **HYBRID** (merged pipeline);
    docstring + `_norm_mode_for_audit` note `hybrid` wire value.
  Verify: pulse-ui `npx tsc --noEmit` OK. `pnpm ai-hero-cli internal lint` at repo root →
    ERR_PNPM_NO_IMPORTER_MANIFEST_FOUND (no root package.json; same as prior iterations).
  Optional rca e2e / full audit: not run this slice; rca-audit.py unchanged.
  Follow-up: old ClickHouse rows may still show `hierarchical` until refresh; db-audit compares latest row.

2026-05-08 — Iteration 5 Ralph: **effort close-out** (docs only, no product code)
  Slice: Mark **PRD** + **TECHNICAL-PLAN** + **Issue 04** triage as **complete**; fix PRD title `#` typo (`/#` → `#`).
  Rationale: Issues **01–05** already shipped (git: 726c1a722 down to 60a748e3c); scratch metadata still said “needs-triage”.
  Verify: `pulse-ui` `npx tsc --noEmit` OK.
  Ralph lint: repo-root `pnpm ai-hero-cli internal lint` → **ERR_PNPM_NO_IMPORTER_MANIFEST_FOUND** (no root package.json);
    `npx ai-hero-cli internal lint` runs but logs **ERROR** — missing `exercises/` directory (tool scans it); workaround: add `exercises/` or ignore for this repo.
  Next: no further tracked slices in `.scratch/rca-flat-hierarchy-segment-merge/issues/`; optional follow-ups remain in PRD (e.g. optional RCA e2e seed/assertions — Issue 05 optional).
