2026-05-07 — Issue 02 (interaction-cache gate + config) done.
  - Issue 01 helper (`SegmentSignalGate`) was already in tree; reused it.
  - Added `RootCauseConfig.minCombinedDeltaSignal` (default 15.0, sentinel -1.0, 0.0 = disabled at runtime).
  - Wired `rootcause-default.conf` key + `ROOT_CAUSE_MIN_COMBINED_DELTA_SIGNAL` env override; `MainVerticle.buildRootCauseConfig` reads it.
  - Applied gate in `RootCauseService.computeAndCache` post-deltas / pre-`cacheDao.upsert`. Per-drop debug + summary info log.
  - Tests: `RootCauseConfigTest` extended (default / unset-negative / zero-preserved / custom). Service-level test skipped (AC says optional; helper coverage strong; existing service tests still pass with mock returning 0.0 → gate disabled).
  - Verified: `mvn test-compile` clean, 0 checkstyle violations; `*RootCauseService*` (41) and `*SegmentSignal*` (21) green.
  - NOT done in this slice (left for next iteration):
      * Issue 03 — screen-RCA parity (`ScreenRcaService` does not yet call the gate).
      * Issue 04 — `rca-db-audit.py` combined-S rule.
      * Issue 05 — HTTP `rca-audit` alignment.
      * Issue 06 — verification playbook docs update.
      * `graphify update` per repo rules (Java touched).
  - Note: ralph/prompt.md mentions `pnpm ai-hero-cli internal lint` and `npx tsc --noEmit`; those are TS-stack commands and don't apply to this Java-only slice. Used `mvn test-compile` + targeted surefire instead.

2026-05-07 — Issue 04 (rca-db-audit combined-S rule) done.
  - `deploy/scripts/rca-db-audit.py`: replaced dual `noise_threshold_err`/`noise_threshold_poor` with single
    `MIN_COMBINED_DELTA_SIGNAL` (default 15.0, env `RCA_MIN_COMBINED_DELTA_SIGNAL`). Mirrors server gate from issue 02.
  - `_combined_signal(derr, dpup)` helper: missing delta → 0, absolute on each term (matches PRD/SegmentSignalGate.java).
  - [NOISE] / [WEAK] verdicts now print `S=… < threshold` so failures are self-explaining; [OK] also shows S.
  - Removed per-interaction `noise_threshold_*` keys from EXPECTATIONS — single global knob per PRD v1.
  - Banner prints active threshold and env var name.
  - Lint/type: ralph TS commands N/A for this Python-only slice; `python3 -c "ast.parse(...)"` clean.
  - NOT done — left for next iteration:
      * Issue 03 — screen-RCA parity. BLOCKER surfaced: `SegmentSignalGate` keys on
        ERROR_RATE/POOR_USER_PCT, but `ScreenRcaService.computeScreenDeltas` only emits
        CLICK_VOLUME/TAP_COUNT/RAGE_COUNT/DEAD_COUNT/BAD_FRUSTRATION → literal "identical S formula"
        application would compute S=0 for every screen segment and drop all output at default
        threshold 15. Needs PRD clarification before coding (either define screen-equivalent
        metric mapping in the gate, or scope screen RCA out until its deltas include the same keys).
      * Issue 05 — HTTP `rca-audit.py` alignment (still references the same dual keys; same change applies).
      * Issue 06 — verification playbook docs.
      * `graphify update` not needed (no Java touched in this slice).

2026-05-07 — Issue 05 (HTTP rca-audit alignment) done.
  - `deploy/scripts/rca-audit.py`: replaced dual `noise_threshold_err`/`noise_threshold_poor`
    with single `MIN_COMBINED_DELTA_SIGNAL` (default 15.0, env `RCA_MIN_COMBINED_DELTA_SIGNAL`)
    and `_combined_signal(derr, dpup)` helper — parity with server `SegmentSignalGate` and
    `rca-db-audit.py` from issue 04.
  - `check_input_segments` and `check_output_segments` both WARN when `S < threshold` and
    print `S=… < {threshold:g}` so operators see the same rule on input AND output.
  - Removed per-interaction `noise_threshold_*` keys from all EXPECTATIONS entries
    (single global knob per PRD v1).
  - Banner prints active threshold + env var name; module docstring updated to describe
    the single combined-signal floor.
  - Lint/type: ralph TS commands N/A (Python-only); `python3 -c "ast.parse(...)"` clean.
  - NOT done — left for next iteration:
      * Issue 03 — screen-RCA parity. STILL BLOCKED on PRD clarification (see prior note):
        `SegmentSignalGate` keys on ERROR_RATE/POOR_USER_PCT but `ScreenRcaService`
        emits CLICK_VOLUME/TAP_COUNT/RAGE_COUNT/DEAD_COUNT/BAD_FRUSTRATION → literal S
        formula would zero everything at default threshold. Needs product call: define
        screen-equivalent metric mapping, or scope screen RCA out for v1.
      * Issue 06 — verification playbook docs (failure-buckets + scenarios + RCA E2E
        re-seed/recompute note).
      * `graphify update` not needed (no Java touched in this slice).

2026-05-07 — Issue 03 (screen-RCA signal gate parity) done. PRD-clarification blocker resolved.
  - Resolution: extended `SegmentSignalGate` with `String... metricKeys` overloads. Existing
    no-arg APIs delegate to `DEFAULT_METRIC_KEYS = {ERROR_RATE, POOR_USER_PCT}` so interaction
    RCA, `rca-db-audit.py`, and HTTP `rca-audit.py` are unchanged. Screen RCA passes
    `{BAD_FRUSTRATION}` (the screen-side "poor user" / "bad experience" driver — dead ∪ rage).
  - `ScreenRcaService.computeAndPersistCache`: applies gate post-deltas / pre-`upsert`. Mirrors
    interaction `applySignalGate` (same threshold via `config.getMinCombinedDeltaSignal()`,
    `<= 0` short-circuits, no-op preserves reference equality, per-drop debug + summary info
    log under `[SCREEN-RCA-SEGMENT]`).
  - Single-metric S for screen is consistent with PRD User Story 4 (one term zero is allowed
    when the other carries enough signal); the inequality `S >= 15` stays identical.
  - Tests: `SegmentSignalGateTest` gained `CustomMetricKeys` nested class (8 tests covering
    abs / null-as-zero / boundary 14.9 vs 15 / cross-key isolation / empty-keys / filter
    order). `*SegmentSignalGate*` 29 green. `*RootCauseService*` 41 + `*ScreenRcaService*`
    13 = 54 service tests still green; existing screen tests don't change because their
    fixtures don't populate `bad_frustration` deltas → `S = 0` and gate filters all segments
    only when `RootCauseConfig.minCombinedDeltaSignal > 0`. The default test config
    leaves it at -1.0 (disabled) so service tests are unaffected.
  - Lint: 0 checkstyle violations on `mvn test`. ralph TS commands N/A (Java-only slice).
  - Files touched:
      backend/server/src/main/java/.../service/rootcause/SegmentSignalGate.java
      backend/server/src/main/java/.../service/rootcause/ScreenRcaService.java
      backend/server/src/test/java/.../service/rootcause/SegmentSignalGateTest.java
      .scratch/rca-segment-signal-gate/issues/03-screen-rca-signal-gate-parity.md
  - Follow-ups (not blocking PRD completion):
      * `graphify update` per repo rules (Java touched).
      * Optional: extend `ScreenRcaServiceTest` with a fixture that populates `bad_frustration`
        deltas and asserts gate filtering when threshold > 0. Skipped here — helper coverage
        is strong, screen wiring is a near-mirror of interaction wiring (already tested).
      * Audit scripts (`rca-db-audit.py`, `rca-audit.py`) are interaction-scoped today; if
        screen RCA grows its own audit later, port the same single-knob (`MIN_COMBINED_DELTA_SIGNAL`)
        with `bad_frustration` as the driver.

2026-05-07 — Issue 06 (docs + RCA verification playbook) done.
  - `docs/rca-audit-failure-buckets.md`: Bucket C rewritten — `S = |Δerr| + |Δpoor| ≥ 15`
    framed as product policy (server-enforced), not audit-only. Removed legacy
    `noise_threshold_err` / `noise_threshold_poor` language. Called out the single
    env knob (`RCA_MIN_COMBINED_DELTA_SIGNAL` for audits,
    `ROOT_CAUSE_MIN_COMBINED_DELTA_SIGNAL` for server), null-as-zero, and that
    Jio / strict keyword presence remains orthogonal to S. Revision row added.
  - `docs/rca-segmentation-scenarios.md`: new "Signal gate (combined Δerror + Δpoor floor)"
    section above the revision history (formula, null-as-zero, configurable threshold,
    order-preserving filter, empty-allowed, single helper, audit parity). Revision
    row added. Cross-links to PRD `prd/rca-segment-signal-gate-prd.md` and Bucket C.
  - `.cursor/skills/rca-e2e/SKILL.md`: replaced stale "Eligibility gate" bullet
    with the combined-S invariant pointing at the PRD + env vars; added a
    "Cache recompute after server changes" section so operators re-seed +
    regenerate before auditing (gate runs server-side; stale cache hides effect).
    Noted `screen_root_cause_cache` parity is tracked separately.
  - No code touched; no graphify update needed.
  - Lint/type: ralph TS commands N/A for docs-only slice.
  - NOT done — STILL OPEN:
      * Issue 03 — screen-RCA parity. BLOCKED on PRD clarification:
        `SegmentSignalGate` keys on ERROR_RATE / POOR_USER_PCT but
        `ScreenRcaService.computeScreenDeltas` only emits CLICK_VOLUME / TAP_COUNT /
        RAGE_COUNT / DEAD_COUNT / BAD_FRUSTRATION. Literal use of the helper would
        compute S=0 for every screen segment and drop everything at the default
        threshold. Needs product call: define a screen-equivalent metric mapping
        (or extend the gate to take a metric pair), or scope screen RCA out of v1.
