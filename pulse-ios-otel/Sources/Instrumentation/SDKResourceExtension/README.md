# SDK Resource extension

This extension captures key application data points and generates a `Resource` object for `TracerProvider`.

## Usage

Use `DefaultResource.get()` to generate an all-in-one resource object.
