Run linting and type checks on the frontend.

1. Change to `pulse-ui/`
2. Run `npx tsc --noEmit` for TypeScript checking
3. Run `yarn lint` for ESLint
4. Report any errors found with file locations
5. If there are auto-fixable issues, offer to run `yarn lint --fix`
