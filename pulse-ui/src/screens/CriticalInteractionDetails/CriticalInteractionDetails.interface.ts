export type CriticalInteractionDetailsFilterValues = {
  PLATFORM: string;
  APP_VERSION: string;
  NETWORK_PROVIDER: string;
  STATE: string;
  OS_VERSION: string;
};

export type TimeFilter = {
  label: string;
  value: string;
};
