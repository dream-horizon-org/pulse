# Pulse Web SDK API key.
# Dev: leave as default — SDK auto-points at http://localhost:4318
# Prod: set to your real project API key from Pulse dashboard
NEXT_PUBLIC_PULSE_API_KEY=default-project_devkey01

# Service version — stamped on every signal as service.version
NEXT_PUBLIC_APP_VERSION=1.0.0
