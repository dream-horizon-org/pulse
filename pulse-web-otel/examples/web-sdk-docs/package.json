{
  "name": "web-sdk-docs",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:all": "yarn --cwd ../.. build && yarn run build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@dreamhorizon/pulse-web": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
