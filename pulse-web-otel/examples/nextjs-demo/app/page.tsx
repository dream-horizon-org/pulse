import React from "react";

export default function HomePage(): React.JSX.Element {
  return (
    <div>
      <h1>Pulse Web SDK — Next.js Demo</h1>
      <p>Navigate using the links above to test screen tracking.</p>
    </div>
  );
}
