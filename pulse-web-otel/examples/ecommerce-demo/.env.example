VITE_PULSE_ENDPOINT_BASE_URL=https://ingest.pulse.io
VITE_PULSE_API_KEY=your-api-key-here
VITE_PULSE_SERVICE_NAME=ecommerce-demo

# "true" → PulseWeb.start({ debugLogRecordLifecycle }) — console log lifecycle: API emit → processors → batch export
# VITE_PULSE_DEBUG_LOG_LIFECYCLE=true
