# Test environment — loaded by Vite when running `--mode test`
# Used by Playwright E2E tests; OTLP calls go to a fake host intercepted by page.route()

# Fast batch scheduler — `@dreamhorizon/pulse-web` defaults to 5000ms (`constants/exporters.ts`)
# without this; M1 specs assume ~200ms flushes (see `e2e/playwright.config.ts`).
VITE_PULSE_BATCH_DELAY_MS=200

# Never load public/pulse-sdk-config.mock.json in E2E — it would overwrite seeded localStorage.
VITE_PULSE_MOCK_SDK_CONFIG=false

VITE_PULSE_API_KEY=default-project_testkey01
VITE_PULSE_SERVICE_NAME=ecommerce-demo-test
