nodeLinker: node-modules

# Keep dependencies under each workspace's `node_modules` (e.g. `examples/nextjs-demo`)
# instead of hoisting everything to the repo-root `node_modules` only.
nmHoistingLimits: workspaces
