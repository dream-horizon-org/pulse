masterHost = "pulse-server-cloudsql-writer"${TEAM_SUFFIX}".dream11"${VPC_SUFFIX}".local"
slaveHost = "pulse-server-cloudsql-reader"${TEAM_SUFFIX}".dream11"${VPC_SUFFIX}".local"
database= "ux_dashboard_master"
username = ${CONFIG_SERVICE_MYSQL_USERNAME}
password = ${VAULT_SERVICE_MYSQL_PASSWORD}