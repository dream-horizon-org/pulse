app {
    pulseServerUrl = ${CONFIG_SERVICE_APPLICATION_PULSESERVERURL}
    shutdownGracePeriod = 5
    serviceJwtSecret = ${?CONFIG_SERVICE_APPLICATION_SERVICEJWTSECRET}
    clickhouseHost = ${CONFIG_SERVICE_APPLICATION_CLICKHOUSEHOST}
    clickhousePort = ${CONFIG_SERVICE_APPLICATION_CLICKHOUSEPORT}
    clickhouseDatabase = ${CONFIG_SERVICE_APPLICATION_CLICKHOUSEDATABASE}
    clickhouseUsername = ${CONFIG_SERVICE_APPLICATION_CLICKHOUSEUSERNAME}
    clickhousePassword = ${CONFIG_SERVICE_APPLICATION_CLICKHOUSEPASSWORD}
    redisHost = ${CONFIG_SERVICE_APPLICATION_REDISHOST}
    redisPort = ${CONFIG_SERVICE_APPLICATION_REDISPORT}
    batchFunnelsEndpoint = ${CONFIG_SERVICE_APPLICATION_BATCHFUNNELSENDPOINT}
    batchJourneysEndpoint = ${CONFIG_SERVICE_APPLICATION_BATCHJOURNEYSENDPOINT}
    batchEventsEndpoint = ${CONFIG_SERVICE_APPLICATION_BATCHEVENTSENDPOINT}
    batchScheduleTime = ${CONFIG_SERVICE_APPLICATION_BATCHSCHEDULETIME}
    batchJobsEnabled = ${CONFIG_SERVICE_APPLICATION_BATCHJOBSENABLED}

    # Usage credits → Redis: POST is async (202); pulse-server dedupes overlapping work. Default 5s.
    usageCreditsSyncIntervalSeconds = 5
    # API keys → Redis: same pattern. Default 600s (10 minutes).
    apiKeysSyncIntervalSeconds = 600
    # Usage-limit emails: POST is async (202); pulse-server uses cron_jobs_history. Default 86400s (24h).
    usageLimitNotificationIntervalSeconds = 86400
}
