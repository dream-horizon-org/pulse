archiver {
  shutdownGracePeriod = 5
  flushIntervalMs = ${?ARCHIVER_FLUSH_INTERVAL_MS}
  flushIntervalMs = 30000
  stagingDir = ${?ARCHIVER_STAGING_DIR}
  stagingDir = "/tmp/pulse-archiver"
  stagingMaxBytes = 2147483648
}
